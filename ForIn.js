const myObject = {
    js : "JavaScrift", 
    cpp : "C++", 
    rb : "roby",
}
for (const key in myObject) {
    // console.log(`${key} is the ${myObject[key]}`)
   
    }
 const   myaObject = [2,3,5,6,3,5]
    
for (const key in myaObject) {
   
      console.log(`${myaObject[key]}`)  
    }


